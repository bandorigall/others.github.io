================================================================
sorter — 뱅드림 캐릭터 소터 개발일지
================================================================
(배포: https://bandorigall.github.io/others.github.io/sorter/   레포: others.github.io)

████ [결정사항 — 금지 항목] 다음 세션이 반드시 지킬 것 ████
  1. 시작 명단을 '밴드끼리 묶는' 배치로 되돌리지 말 것. (2026-08-01 사용자 지시)
     밴드 군집은 '밴드 단위로 취향이 갈리는 사람'에게만 이득인데 실제로는 아님.
  2. merge() 의 동점 링크 검사(다음 캐릭터가 실제 그 짝인지 확인)를 빼지 말 것.
     빼면 원본처럼 7% 빨라지지만 순위가 틀어진다.
  3. pick() 의 state.swapped 되돌리기 로직을 건드리면 순위가 통째로 뒤집힌다.
     수정 시 반드시 rank_test 로 검증할 것.
  4. assets/ 와 data.js 는 자동생성물. 직접 수정 금지 (build_assets.py 로 재생성).
  5. build_assets.py 의 animeface.xml ASCII 임시경로 복사 로직을 없애지 말 것.
     OpenCV 는 한글 경로 파일을 못 연다(레포 경로가 한글).
  6. 사용자가 제외 요청한 부가 캐릭터를 임의로 되살리지 말 것(아래 목록 참조).
  7. 기본 모드는 '월드컵'이다(2026-08-01 사용자 지시). state.mode 초기값 'cup',
     startNew() 폴백 'cup', Enter 키 = 월드컵, 시작화면 '추천' 배지도 월드컵.
     '전체'를 다시 기본으로 돌리지 말 것.
  8. 모드는 '전체'와 '월드컵' 두 가지뿐이다. TOP10/TOP20(상위 K명) 모드는
     2026-08-01 사용자 지시로 삭제했다. 되살리지 말 것.
     월드컵은 '진짜 월드컵' — 순위·준우승 표시 없이 라운드(x강)와 우승자만 보여준다.

■ 현재 상태 (2026-08-01 기준, 완성·배포됨 / others 리포 7dfe896 까지 푸시 완료)
  - 캐릭터 이지선다 → 순위표. 기본 60명(밴드 12개 × 5명), 부가 13명은 옵션.
  - 화면 3개: 시작(밴드 선택 + 모드 선택) / 대결 / 결과.
  - 모드 2개: 전체(순위표) · 월드컵(우승자 1명). 2026-08-01 TOP10/TOP20 삭제.
  - 기능: 비김 · 되돌리기 · 중간저장(이어하기) · 밴드 필터 · 키보드 전용 조작
          · 결과 PNG 저장 · PNG 클립보드 복사 · 순위 텍스트 복사.
  - 모바일 대응(카드 좌우 2분할, safe-area), 단축키 배지는 데스크톱에서만 노출.
  - 모바일 최적화(2026-08-01 2차):
      · #screen-battle 을 세로 flex + min-height:100dvh, .arena 가 남는 높이를 먹는다.
        대결 중엔 :has() 로 .site-footer 를 숨긴다(안 그러면 100dvh+푸터라 늘 스크롤).
      · 가로 넘침은 .screen { overflow-x: clip } 으로만 막는다.
        ★[금지] html/body 에 overflow-x:hidden 을 걸지 말 것 — 모바일에서 세로 스크롤이
          통째로 죽는다(2026-08-01 실기기에서 확인하고 되돌림).
      · ≤560px: 밴드칩/퀵칩 터치 높이 확대, 순위표 1열 + 밴드명 생략, 결과 버튼 2열 그리드.
      · ≤560px 결과화면(2차 수정): 상위 토너먼트를 가로 스크롤 대신 라운드를 세로로 쌓는다.
        위에서부터 결승 → 4강 → 8강(우승 카드 바로 아래에서 이어지는 순서),
        각 라운드는 2열 그리드, 결승만 1열로 크게. 3칸을 폰 폭에 밀어넣으면 얼굴이 손톱만 해진다.
      · 전체 대진표는 <details id="full-bracket"> 로 감쌌고 폭 780 이하면 접어서 시작한다
        (renderResult 에서 open 지정). 예선 28경기가 폰에서 화면을 다 잡아먹었다.
      · 가로모드(max-height:520px): 카드 46dvh, .arena .c-band 숨김, 여백 축소.
        ※ .c-band 는 우승 카드에도 쓰이므로 반드시 .arena 로 범위를 좁힐 것.

■ 파일 구조
  characters.json            [레포 최상단] 캐릭터 마스터. 모든 모듈 공용 원천
  build_characters.py        [레포 최상단] characters.json 재생성기(네트워크 안 씀)
  character_assets/          외부 수집 초상 원본(신캐 15 + 부가 13 + 미사키 교체분)
  others/sorter/
    build_assets.py          얼굴 크롭 → webp 변환 + data.js 생성
    animeface.xml            lbpcascade_animeface (얼굴 검출 캐스케이드)
    data.js / assets/        자동생성물
    app.js / index.html / styles.css / nav.js
    _crop_check.png          --check 로 뽑는 크롭 대조 시트(gitignore)

■ 정렬 알고리즘
  공통 구조 = '답변 로그 재생'.
    비교 결과를 answers 배열에 쌓고, 화면을 그릴 때마다 알고리즘을 처음부터 다시 돌린다.
    답 없는 쌍을 만나면 NEED 예외를 던져 그 쌍을 질문으로 띄운다.
    → 되돌리기 = 로그 pop 후 재실행, 이어하기 = 로그만 저장. 재계산 1ms 미만.
  모드 (2가지뿐)
    full : 상향식 병합정렬. 전체 정확 정렬. 60명 기준 약 280문항.
    cup  : 진짜 월드컵(단판 탈락 토너먼트). runCup() 한 방으로 우승자 1명만.
           인원이 2의 거듭제곱이 아니면 예선으로 먼저 줄인다.
             pow = n 이하 최대 2^k, extra = n-pow → 예선 extra 경기(앞쪽 2*extra명),
             나머지는 부전승 → pow강 → … → 결승. 총 경기수는 항상 n-1 (60명=59).
           라운드 표시는 NEED.round(size/no/total/prelim)를 실어 던져서 만든다.
           '예선 / 32강 / 16강 / 8강 / 4강 / 결승'. 비김 버튼은 숨긴다(월드컵엔 비김 없음).
           ※ 취향이 추이적이면 단판 토너먼트 우승자는 실제 1위와 항상 일치한다(테스트로 확인).
           ※ 결과 화면 상단은 #cup-top 2단 그리드 = 왼쪽 #champion 카드,
             오른쪽 #top-bracket('상위 토너먼트'). 상위 토너먼트는 예선을 빼고
             size<=8 라운드(8강·4강·결승)만 그린다. 인원이 적으면 있는 라운드만.
             ★칸 순서는 뒤집어 그린다(왼쪽부터 결승 → 4강 → 8강). 왼쪽 우승 카드로
               좁아지며 이어지는 모양이라야 유기적으로 보인다(2026-08-01 사용자 지시).
               별도 '우승' 칸은 두지 않는다(우승 카드와 중복).
             라운드 j번 경기 승자가 다음 라운드로 순서대로 가므로 기록 순서대로 칸을
             세우면 그대로 대진표가 되고, .tb-col 의 space-around 로 세로 가운데가 맞는다.
             예선 포함 전체는 아래 #bracket 에 그대로 남겨둔다(groupRounds() 공용).
           ※ 결과 화면은 #champion 카드 + #bracket(전체 대진표), 순위표는 숨김.
             모든 경기를 state.matches 에 {size,prelim,a,b,w} 로 기록해두고
             라운드별 세로 칸 → 가로 스크롤로 그린다. 이긴 쪽만 밴드색으로 켠다.
             '우승까지의 길'(#bracket-path)은 우승자가 이긴 상대만 뽑은 요약.
             PNG(buildCupCanvas)도 화면과 같은 구성 — 왼쪽 우승 카드(420px) +
             오른쪽 drawTopBracket()으로 결승·4강·8강을 썸네일까지 그린다(1080x858).
             라운드가 없을 만큼 인원이 적으면 우승 카드를 가운데 크게 + '우승까지의 길' 글로.
             ※ withImages()가 우승자 카드이미지(_im)와 대진표 썸네일(_imT)을 따로 캐시한다.
               대진표 캐릭터 썸네일을 안 받아두면 캔버스에 얼굴이 안 나온다.

■ 알고리즘 조사 기록 — 다시 뒤집지 말 것
  1) 참고 사이트(garupasorter.tumblr.com) 코드를 받아 확인:
     2008년 K-Factory 제 소터로, **토너먼트가 아니라 병합정렬**이다.
     (fnc_Sort/fnc_CountUp = 머지, ary_EqualData = 비김, back_* = Undo)
     ※ main.js 는 UTF-16, data.js 는 Shift-JIS(cp932). 인코딩 주의.
     ※ 원본 데이터는 9밴드 45명(아베무지카까지).
  2) 정보이론 하한: log2(60!) = 272.1 → 전체 순위 확정에 최소 273회.
     현재 평균 282회는 하한 대비 +3.3%로 사실상 최적.
     "전체 순위를 유지하며 질문을 크게 줄이는 방법은 원리적으로 없다."
  3) Elo + 스위스 페어링(근사) 시뮬레이션 200회 — 채택하지 않음:
        라운드 질문수 스피어만 1위적중
          4    120    0.842    31%
          5    150    0.885    54%
         10    300    0.958   100%
     300문항을 써도 병합정렬(282문항·완전정확)보다 나쁘다.
  4) 브래킷 반복(상위 K 정확) — 한때 TOP10/TOP20으로 넣었다가 2026-08-01 제거.
        K=3 94 / K=5 116 / K=10 153 / K=20 213 / 전체 282 (정확도 100%)
        "상위 10/20위" 라는 어중간한 결과보다 '전체 순위' 아니면 '월드컵 우승자'
        둘 중 하나가 낫다는 사용자 판단.

■ 질문 수 — 원본 엔진 직접 구동 결과 ★중요
  사용자가 "원본은 45명에 137회"라고 해서 원본 엔진을 node 로 그대로 돌려 확인.
  사용자 말이 맞았고 원인은 두 가지였다.
  (1) 비김의 동점 링크가 누적된다
      원본은 ary_EqualData 로 묶인 캐릭터를 이후 모든 병합에서 질문 없이 딸려보낸다.
      state.eqLink + merge() 의 drain() 으로 이식. 60명 기준:
          비김률  0%   10%   20%   35%
          질문수 282   226   198   177
      ※ 원본은 링크만 보고 무조건 딸려보내 순서가 틀어질 수 있으나,
        우리는 다음 캐릭터가 실제 그 짝인지 확인한다(7% 손해, 정확도 우선).
      ※ TOP 모드(브래킷)에서는 비김이 오히려 질문을 늘린다(TOP20 214→243).
        안내 문구는 '전체 모드에서 비김이 이득'이라고만 적을 것.
  (2) 원본은 캐릭터를 섞지 않는다 (main.js/data.js 전체 grep 으로 확인.
      Math.random 은 '어떤 일러스트를 보여줄지' 고르는 2군데뿐)
      항상 밴드 순서로 시작 → 취향이 그 순서와 비슷하면 질문이 급감.
      원본 45명 실측: 목록순서와 동일 136 / 밴드순서 유지 147 / 밴드끼리만 뭉침 169
                      / 완전 무작위 193
      우리도 밴드 묶음을 시도해 60명에서 282→248(밴드뭉침), →210(순서일치)을 확인했으나,
      사용자가 "밴드 클러스터는 무의미하다"고 판단해 제거. 지금은 셔플만 한다.
      ※ 그냥 '셔플만 제거'하면 characters.json 순서(=생일순)라 밴드가 뒤섞여 이득 없음(283→281).
  (3) 좌우 배치 뒤집기
      병합정렬은 한쪽을 고정한 채 상대만 바꿔 물어서(마호로 vs 카스미 → 마호로 vs 타에)
      자리까지 고정되면 편향돼 보이고 지루하다. 쌍+진행도 해시로 좌우를 뒤집되
      되돌리기 시 같은 배치가 재현되도록 결정적으로 계산(state.swapped).
      실측: 연속 질문에 남은 캐릭터가 같은 자리에 또 오는 비율 100% → 48.2%,
            왼쪽 카드 선택률 48.7%(편향 없음).

■ UI 수정 이력 (2026-08-01, 사용자 지적)
  - 대결 카드가 너무 컸다: .arena max-width 620px + .char-card max-width 260px,
    .card-img max-height 38dvh 로 묶었다. 세로 짧은 화면에서도 카드 2장+버튼이 한 화면.
  - 밴드색을 글자색으로 그대로 쓰면 어두운 밴드(#881188 로젤리아, #CC0000 RAS,
    #EE0022 애프터글로우 등)가 검은 배경에 묻혀 안 보인다.
    → CSS 는 color-mix(... 55%, #fff), 캔버스는 lighten() 으로 흰색을 섞어 쓴다.
      [규칙] 앞으로도 밴드색은 '테두리·막대·배경'에만 그대로 쓰고, 글자에 쓸 땐 반드시 밝힐 것.

■ 이미지 파이프라인
  - 전신 아트를 그대로 쓰면 무겁고 얼굴이 작다 → lbpcascade_animeface 로 얼굴 상자를
    잡고 '머리 전체 + 어깨'만 정사각 크롭.
      한 변 = 얼굴높이 * ZOOM(2.70), 얼굴 위 여백 = 얼굴높이 * UP(0.95)
      얼굴 검출 실패 시 알파 폭 프로파일로 어깨선 추정(폴백)
  - 카드 320px(평균 20.6KB) / 결과표 썸네일 112px(평균 4.2KB). 전체 약 1.5MB + 0.3MB.
    대결 화면은 2장씩 lazy + 다음 2장 프리페치, 결과표는 썸네일만 사용.
  - build_assets.py --check 로 전원 크롭 대조 시트를 뽑아 눈으로 검수할 것.
  - 오쿠사와 미사키: MBTI 초상이 미셸(곰인형)이라 곰 얼굴이 잡혔다
    → build_characters.py 의 PORTRAIT_OVERRIDE 로 fandom 초상 교체.

■ 캐릭터 데이터
  - 밴드 60명 = 12밴드 × 5명 (포피파/애프터글로우/파스파레/로젤리아/HHW/모르포니카/
    RAS/마이고/아베무지카/뮤타입/millsage/一家Dumb Rock!)
    초상 출처: 45명은 others/bangdream_mbti_korean, 신캐 15명은 유메미타 공식(5) +
    bandori.fandom Our Notes 아트(10).
  - 부가 캐릭터 13명 (optional=true, 소터에서 기본 꺼짐 / 점선 칩):
      토야마 아스카(카스미 여동생) · 츠즈키 시후네(SPACE 오너·라나 할머니)
      츠키시마 마리나(CiRCLE) · 스미타 마나(sumimi)
      미타케 란 아버지 · 유키나 아버지
      비올라 · 벨 · 포포 · 유메미타 매니저 (유메미타)
      마츠기 리리코(SPACE 직원) · 샤를로트 루이즈 미소노 · 츠키노모리 교장
    켜면 73명, 전체 모드 약 330문항. 시작화면 [기본] 버튼으로 60명 복구.
  ★ 유메미타 매니저 초상은 방갤 움짤(no=6374677)에서 프레임을 뽑아 만들었다.
      공식 사이트는 복셀(픽셀) 아트만 제공하고 fandom 엔 아직 페이지가 없다.
      절차: 게시글 HTML → dcimg 링크(Referer 헤더 필요) → 애니메이션 webp 프레임 추출
            → 자막 영역(아래 22%) 잘라냄 → 노란 배경을 가장자리 플러드필로 투명화
            → 알파 최대 덩어리만 남겨 잡티 제거 → bbox 크롭.
      더 좋은 컷이 생기면 character_assets/manager.png 만 교체하고 재빌드하면 된다.
  ※ 제외한 캐릭터와 이유:
      유키나 어머니 / 토야마 카오리 / 이치가야 마미 / 하나조노 노노에 / 야마부키 준
        → 팬덤 이미지가 애니 스크린샷이라 배경 네모가 남아 카드 톤이 깨짐.
          깔끔한 초상이 생기면 추가 가능.
      하루미 · 미셸 · 마리 안드로메다 · 히가시나카노 나오 · 아유미 · 니콜리나 · 아카리
        → 2026-08-01 사용자가 명시적으로 제외 요청. [금지] 임의로 되살리지 말 것.
  ※ 팬덤 Category:Characters 에 167명, 그중 150명이 초상 보유.
    추가는 build_characters.py 의 SUB_CHARACTERS 에 (한글명, slug, 설명) 한 줄이면 됨.

■ 키보드 조작
    시작 : 1=월드컵  2/Enter=전체
    대결 : ←/A/1 왼쪽  →/D/2 오른쪽  Space/↓/S/3 비김  Z/Backspace 되돌리기
    결과 : S 이미지저장  C 텍스트복사  R 다시하기
  한글 자판(ㅁㅇㄴㅋ)도 받는다. Ctrl/Cmd/Alt 조합은 무시(브라우저 단축키 보호).
  ★ 키가 있어도 안 보이면 아무도 안 쓴다 — 카드·버튼에 단축키 배지를 표시하되
    @media (hover:hover) and (pointer:fine) 로 감싸 모바일에선 숨긴다.

■ 결과 이미지(PNG)  ★2026-08-01 변경: 화면 스냅샷 방식이 1순위
  shot() = buildDomCanvas() 실패 시 withImages()(옛 캔버스 그림)로 자동 폴백.
  buildDomCanvas: #screen-result 복제 → 버튼/토스트/전체 대진표/안내문 제거 →
    <img> 전부 data URI 화(외부 참조가 하나라도 있으면 SVG 가 안 그려진다) →
    styles.css 본문을 <style> 로 넣고 <foreignObject> 에 담아
    data:image/svg+xml 이미지로 만들어 2배 크기 캔버스에 그린다. 라이브러리 없음.
    높이는 화면 밖(position:fixed;left:-10000px) 에 잠깐 붙여 실측한다.
    XMLSerializer 로 직렬화해야 XML 로 파싱된다(<img> 자동 닫힘 처리).
    ※ 폰트: SVG 안에서는 외부 웹폰트를 못 받아 시스템 폰트로 대체되고, 그래서
      화면과 글자 굵기·모양이 달라 '깨져' 보인다. → fontCss() 로 구글폰트를
      text= 파라미터(그 이미지에 실제로 쓰인 글자만) subset 요청 → woff2 를 data URI 로
      심어 넣는다. 몇십 KB. 실패하면 폰트 없이 그냥 그린다.
    ※ 손으로 좌표를 계산하던 캔버스 그림이 PC 에서 깨져 보인다는 지적으로 바꿨다.
    ※ 이미지 폭은 화면과 무관하게 1080 고정. foreignObject 안의 미디어쿼리는
      SVG 뷰포트(=이 폭) 기준이라, 폰에서 찍어도 PC 배치(2단)로 나온다.
    ★[함정] SVG 안에는 <html>/<body> 가 없다. styles.css 는 글자색·폰트를 html,body 에,
      변수를 :root 에 걸어두므로 그대로 두면 스냅샷이 통째로 검은 글씨가 된다.
      → extra CSS 에서 .shot-root 에 색·폰트·CSS 변수를 직접 다시 박는다. 지우지 말 것.
  모바일 저장/복사
    - 저장: navigator.canShare({files}) 되면 공유 시트(사진 앨범 저장) 우선, 아니면 다운로드.
    - 복사: 사파리는 클릭 직후 동기적으로 clipboard.write 를 불러야 허용하므로
      Promise<Blob> 을 그대로 ClipboardItem 에 넘긴다(기다렸다 쓰면 iOS 에서 실패).
  (폴백용) 예전 방식 — 외부 라이브러리 없이 Canvas 2D 로 직접 그린다.
  1080px 폭, 인원에 따라 1~3열, 밴드색 막대 + 썸네일 + 등수 + 이름 + 밴드.
  CDN 의존이 없어 오프라인·CSP 환경에서도 안전.

■ 자동 점검 — selftest.sh  ★사람이 눈으로 확인하지 말고 이걸 돌릴 것
  ./selftest.sh          (--shots 를 붙이면 _shots/ 에 스크린샷도 저장)
  헤드리스 크롬으로 실제 페이지를 띄워서:
    ① 가로 넘침(scrollWidth > clientWidth) 이 없는지 — 1280/700/485 폭, cup·full 둘 다
    ② 결과 이미지가 PC·모바일에서 같은 크기인지(폭 1080 고정) + 높이가 과하지 않은지
       → 다르면 '아래 빈 공간' 버그가 되살아난 것
    ③ 결과 이미지 제목줄에 밝은 픽셀이 있는지 = 검은 글씨로 떨어지지 않았는지
  이걸 가능하게 하는 훅(app.js 초기화 끝부분, 파라미터 없으면 아무 일도 안 함):
    ?autotest=cup|full   자동으로 끝까지 답해 결과 화면까지 감
    &shot=1              결과 이미지를 만들어 화면을 그 이미지로 교체(title 에 크기 표시)
    &colorcheck=1        위쪽 밝은 픽셀 수를 세어 TEXTOK / TEXTBLACK 을 title 에
    &diag=1              vw / sw 와 삐져나온 요소 목록을 title 에
  ※ 헤드리스 크롬은 --window-size 를 줘도 윈도우 최소 폭 때문에 CSS 폭이 485 밑으로
    안 내려간다. 485 도 ≤560 미디어쿼리에 걸리므로 모바일 배치 검증은 된다.
    (390px 실기기와 완전히 같지는 않다는 점만 감안)

■ 테스트 (스크립트는 임시폴더에 있었으므로 필요하면 재작성)
  최소 DOM 스텁으로 app.js 를 실제 구동하는 통합 테스트가 핵심이다.
    rank_test  : 전체 모드 20회 완주 → 순위 불일치 0/20 (2026-08-01 재확인 OK)
    cup_test   : 월드컵 20회 완주 → 매번 정확히 59문항, 비김 버튼 숨김,
                 라운드 표기 예선→32강→16강→8강→4강→결승, 우승자 = 실제 1위 20/20
    resume_test: 30문항 진행 → 재부팅 → 이어하기 → 완주 결과 정확
    key_test   : 키보드만으로 4가지 조합 완주
    tie_test   : 비김률별 질문 수
    side_test  : 좌우 뒤집기 비율
    sub_test   : 부가 캐릭터 기본 꺼짐 / 켜면 73명 / [기본] 복구
  ★ 테스트에서 정답 순위를 만들 때 반드시 optional 캐릭터를 제외할 것.
    (안 그러면 게임에 없는 캐릭터가 정답에 껴서 전부 실패로 나온다)
  ★ 과거에 잡은 실제 버그: 모드 도입 후 EST_TOTAL 전역을 MODES[].est 로 바꿨는데
    '이어하기' 진행률 계산 한 곳이 옛 이름을 참조해, 저장 기록이 있을 때만
    시작 화면이 죽었다. 저장이 걸린 경로는 반드시 테스트할 것.

■ 이번 세션(2026-08-01 3차)에 끝낸 것 — 다시 건드리지 말 것
  - 결과 화면 상단 2단(우승 카드 ← 결승·4강·8강) / 월드컵 기본 모드화
  - 결과 이미지 = 화면 스냅샷(DOM→SVG→캔버스), 폰트 subset 임베드, 폭 1080 고정
  - '검은 글씨' 두 건 모두 원인 규명·해결(.shot-root 에 색·폰트·변수 재지정)
  - 이미지 아래 빈 공간 해결(폭 1080 iframe 에서 높이 측정)
  - 모바일: 대결 화면 100dvh 레이아웃, 결과 상위 토너먼트 세로 배치, 전체 대진표 접기
  - selftest.sh 로 위 항목 전부 자동 검증(마지막 실행 전부 통과)

■ 다음 할 일
  - 사용자 확인 대기: 실기기(폰) 에서 결과 화면·이미지 저장/복사.
    헤드리스는 CSS 폭 485 밑으로 못 내려가 390px 실기기 검증은 아직 안 됐다.
  - 신캐 15명·부가 13명은 개별 캐릭터 컬러가 없어 밴드색을 대신 쓰는 중.
    공식/팬덤에서 개별 색이 확인되면 build_characters.py 에 반영.
  - 결과 URL 공유(순위 인코딩) — 요청 있으면 추가.
  - 유메미타 매니저 초상: 더 선명한 컷이나 공식 아트가 나오면 교체.

■ 2026-08-01 4차
  - [버그수정] PC(윈도우)에서 이미지 저장 시 윈도우 공유 시트가 뜨던 문제.
    원인: Windows Edge/Chrome도 navigator.canShare(files)를 지원 → 공유 분기로 빠짐.
    해결: app.js savePng() 에서 터치기기 조건 추가
      (matchMedia('(pointer: coarse)').matches && navigator.maxTouchPoints > 0)
    → PC는 항상 a[download] 직접 저장, 모바일만 공유 시트.
  - [결정/금지] 이미지 저장은 PC·모바일 **모두** navigator.share 안 씀. 공유 시트 분기 완전 삭제.
    savePng() = shot() -> toBlob -> a[download] 만. 다시 공유 시트 넣지 말 것.
  - 파일명에 날짜시간 꼬릿말: bangdream_sorter_YYYYMMDD_HHMMSS.png (로컬 시각).
  - 푸시 완료(others.github.io main 56b94d1). 소터는 상위 BangDream 리포가 아니라
    others/ 자체 리포(bandorigall/others.github.io)이며 others/git_auto.sh 로 푸시한다.
  - [전체 순위표 차등 강조] rank-list 를 3단으로: 1~3위 / 4~10위 / 11위 이하.
    app.js renderResult() 가 li 에 tier-1/2/3 클래스 + 구분 헤더(li.tier-sep, "TOP 3"/"TOP 10"/"그 외") 삽입,
    styles.css 에서 단계별로 카드 패딩·썸네일(56/46/38px)·글자·색 차등. 1~3위는 2단 배치에서도 한 줄 통째 사용.
    selftest.sh 전부 통과(가로 넘침·이미지 크기·글자색).
